# Lesson 2: Fuzzy C-Means Hands-on

- Use `src/fuzzy_clustering.py` to fit fuzzy c-means
- Visualize membership degrees for sample customers
- Compare with KMeans from `sklearn.cluster`

Exercises:

- Compute FPC (fuzzy partition coefficient)
- Report differences in segment assignment for selected users
